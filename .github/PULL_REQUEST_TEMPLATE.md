## Summary

Describe the change and its operational impact.

## Safety checklist

- [ ] No Xiaomi Account credentials, 2FA codes, cache files, tokens, logs, private IPs, device IDs, or e-mail addresses are included.
- [ ] `python -m pytest` passes locally.
- [ ] `python -m ruff check .` and `python -m ruff format --check .` pass locally.
- [ ] The documentation/examples use fictional values only.
